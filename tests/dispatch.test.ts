import {describe,expect,it} from "vitest";import {selectEligibleDrivers,type DriverCandidate} from "../lib/domain";
const base:DriverCandidate[]=[{id:"guard",status:"available",accountStatus:"active",priority:9,lastAssignedAt:"2026-09-06T10:00:00Z",onDuty:true,acceptsFuture:true,accessible:false},{id:"first",status:"available",accountStatus:"active",priority:1,lastAssignedAt:"2026-09-06T11:00:00Z",onDuty:false,acceptsFuture:true,accessible:true},{id:"fair",status:"available",accountStatus:"active",priority:1,lastAssignedAt:"2026-09-05T11:00:00Z",onDuty:false,acceptsFuture:true,accessible:false},{id:"busy",status:"busy",accountStatus:"active",priority:0,lastAssignedAt:null,onDuty:true,acceptsFuture:true,accessible:true},{id:"suspended",status:"available",accountStatus:"suspended",priority:0,lastAssignedAt:null,onDuty:true,acceptsFuture:true,accessible:true}];
describe("DispatchEngine",()=>{it("prioriza guardia disponible",()=>expect(selectEligibleDrivers(base,{scheduledFor:null,accessible:false})[0].id).toBe("guard"));it("ignora guardia ocupada y suspendidos en inmediatos",()=>expect(selectEligibleDrivers(base,{scheduledFor:null,accessible:false}).map(d=>d.id)).toEqual(["guard","fair","first"]));it("usa round-robin entre misma prioridad",()=>expect(selectEligibleDrivers(base.filter(d=>!d.onDuty),{scheduledFor:null,accessible:false}).map(d=>d.id).slice(0,2)).toEqual(["fair","first"]));it("filtra taxi adaptado",()=>expect(selectEligibleDrivers(base,{scheduledFor:null,accessible:true}).map(d=>d.id)).toEqual(["first"]));it("permite ocupado en reserva futura",()=>expect(selectEligibleDrivers(base,{scheduledFor:"2026-09-07T10:00:00Z",accessible:false}).some(d=>d.id==="busy")).toBe(true))});
