"use client";import {useState} from "react";import {createClient} from "@supabase/supabase-js";
export default function AuthForm(){const [message,setMessage]=useState("");async function login(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget),url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key){setMessage("El acceso real se activará al configurar Supabase. Usa /admin o /taxista para la demo.");return}const db=createClient(url,key);const {data:result,error}=await db.auth.signInWithPassword({email:String(data.get("email")),password:String(data.get("password"))});if(error){setMessage("Email o contraseña incorrectos.");return}document.cookie=`ptc_access_token=${result.session.access_token}; Path=/; SameSite=Lax; Secure; Max-Age=3600`;location.href="/taxista"}return <form className="booking-form auth-form" onSubmit={login}><label>Email<input name="email" type="email" required/></label><label>Contraseña<input name="password" type="password" required/></label>{message&&<p className="error">{message}</p>}<button className="submit-button">Entrar</button></form>}
