"use client";import {useState} from "react";const fields=[["punctuality","Puntualidad"],["treatment","Trato"],["cleanliness","Limpieza del vehículo"],["driving","Conducción"],["overall","Experiencia general"]];export default function RatingForm({token}:{token:string}){const [done,setDone]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(process.env.NEXT_PUBLIC_DEMO_MODE!=="false"){setDone(true);return}const data=Object.fromEntries(new FormData(e.currentTarget));const response=await fetch(`/api/ratings/${token}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(response.ok)setDone(true);else setError("Este enlace no es válido o ya fue utilizado.")}if(done)return <div className="tracker-card"><div className="status-orb accepted">✓</div><h1>Gracias por tu valoración</h1><p>Tu opinión es privada y solo la revisará la administración.</p></div>;return <form className="booking-form rating-form" onSubmit={submit}>{fields.map(([name,label])=><fieldset key={name}><legend>{label}</legend><div className="stars">{[1,2,3,4,5].map(n=><label key={n}><input type="radio" name={name} value={n} required/><span>{n} ★</span></label>)}</div></fieldset>)}<label>Comentario <small>Opcional</small><textarea name="comment" rows={3}/></label><fieldset><legend>¿Quieres comunicar alguna incidencia?</legend><div className="inline-radios"><label><input type="radio" name="incident" value="false" defaultChecked/> No</label><label><input type="radio" name="incident" value="true"/> Sí</label></div></fieldset><label>Describe la incidencia <small>Opcional</small><textarea name="incidentText" rows={3}/></label>{error&&<p className="error">{error}</p>}<button className="submit-button">Enviar valoración</button></form>}
