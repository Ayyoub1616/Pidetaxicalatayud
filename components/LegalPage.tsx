import {Logo} from "./Logo";import Link from "next/link";export default function LegalPage({title,children}:{title:string;children:React.ReactNode}){return <main><header className="compact-header shell"><Logo/><Link href="/">Volver</Link></header><article className="content-page"><span className="section-kicker">Información</span><h1>{title}</h1><div className="legal-warning">Texto provisional. Debe revisarse y completarse con los datos reales del titular antes del lanzamiento.</div>{children}</article></main>}
