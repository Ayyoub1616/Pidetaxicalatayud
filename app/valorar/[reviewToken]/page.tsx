import RatingForm from "../../../components/RatingForm";import {Logo} from "../../../components/Logo";export default async function Page({params}:{params:Promise<{reviewToken:string}>}){const {reviewToken}=await params;return <main className="form-page"><header className="compact-header shell"><Logo/></header><section className="form-shell"><span className="section-kicker">Valoración privada</span><h1>¿Qué tal fue tu viaje?</h1><p>Tardarás menos de un minuto. Solo la administración verá tus respuestas.</p><RatingForm token={reviewToken}/></section></main>}
