import {notFound} from "next/navigation";import AdminSection from "../../../components/AdminSection";import {requireRole} from "../../../lib/auth";const sections=["servicios","taxistas","guardias","valoraciones","incidencias","estadisticas","configuracion"];export default async function Page({params}:{params:Promise<{section:string}>}){await requireRole(["ADMIN","CITY_ADMIN","SUPERADMIN"]);const {section}=await params;if(!sections.includes(section))notFound();return <AdminSection section={section}/>}
