import DriverApp from "../../components/DriverApp";import {requireRole} from "../../lib/auth";export const metadata={title:"App para taxistas | PideTaxiCalatayud",robots:{index:false,follow:false},manifest:"/taxista/manifest.webmanifest"};export default async function DriverPage(){await requireRole(["DRIVER","ADMIN","CITY_ADMIN","SUPERADMIN"]);return <DriverApp/>}
