import {createClient} from "@supabase/supabase-js";const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,email=process.env.INITIAL_ADMIN_EMAIL;if(!url||!key||!email)throw new Error("Configura NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY e INITIAL_ADMIN_EMAIL");const db=createClient(url,key,{auth:{persistSession:false}});const {data:city,error:cityError}=await db.from("cities").select("id").eq("name","Calatayud").single();if(cityError)throw cityError;const {data,error}=await db.auth.admin.inviteUserByEmail(email,{redirectTo:`${process.env.NEXT_PUBLIC_SITE_URL}/acceso`});if(error)throw error;const {error:profileError}=await db.from("profiles").upsert({id:data.user.id,role:"ADMIN",city_id:city.id,full_name:"Administrador"});if(profileError)throw profileError;console.log(`Invitación de administrador enviada a ${email}`);
